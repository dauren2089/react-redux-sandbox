import React from "react";
import Counter from "./counter";

const App = () => {
    return (
        <Counter
            // counter={store.getState()}
                // inc={inc}
                // dec={dec}
                // rnd={ () => {
                //     const rndNbr = Math.floor(Math.random()*10);
                //     rnd(rndNbr);
                // }}
        />
    )
}

export default App;